/**
 * Legacy config file (previously forced AI Thinker pinout).
 *
 * The camera sketch now auto-detects pin mappings at runtime, so this file is kept only
 * to avoid breaking includes in older copies.
 */
#ifndef BOARD_CONFIG_H
#define BOARD_CONFIG_H

// (intentionally empty)

#endif
